node_modules/
_site/
.cache/
.DS_Store

# Planning documents and working notes: kept locally, deliberately not published.
portfolio-site-plan.md
claude-code-build-brief.md
NOTES.md

# Obsidian: keep the vault settings that make authoring work, ignore the rest
# (workspace layout, plugin state, caches — all machine-specific noise).
src/.obsidian/*
!src/.obsidian/app.json
